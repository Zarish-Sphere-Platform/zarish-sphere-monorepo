{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "Unified entry point for Zarish Sphere microservices (Routing, Auth, Data).",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo 'No tests yet for API Gateway' && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "@zarish/core-engine": "workspace:^" 
  }
}
